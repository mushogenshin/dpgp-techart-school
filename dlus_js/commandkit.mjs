import { defineConfig } from 'commandkit';

export default defineConfig({
  src: 'src',
  // main: 'index.js',
  main: 'firestore/send_mail.js',
  // watch: false // use this to disable watch mode
});
